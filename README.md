# Alarm Clock

This is alarm clock application. It has alarm, clock, stopwatch, timer and world clock functionalities.

To install open command line and paste: 
pip install -i https://test.pypi.org/simple/ --extra-index-url https://pypi.org/simple/ alarm-clock==1.5
To run type in command line:
python -m alarm_clock

To using Abstract API function in "Aktualny czas na świecie" tab, set up environment variable API_KEY which will contain
your api key from https://www.abstractapi.com/api/time-date-timezone-api.
Examples how to set up environment variable in different operating systems: 
https://www.twilio.com/en-us/blog/how-to-set-environment-variables-html